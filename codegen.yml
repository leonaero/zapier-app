overwrite: true
schema: "./schema.json"
documents: "./src/fixtures/queries/*.js"
generates:
  src/types/api.js:
    plugins:
      - "flow"
      - "flow-operations"
config:
  scalars:
    FlightNid: number
